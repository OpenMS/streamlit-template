name: Sync Conda Environment

on:
  push:
jobs:
  update-requirements:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Conda
        uses: conda-incubator/setup-miniconda@v3
        with:
          activate-environment: template
          python-version: "3.10"
          auto-activate-base: false
          channels: conda-forge,bioconda,defaults

      - name: Install requirements.txt in Conda environment 
        run: | 
          pip install -r requirements.txt
      - name: Export to environment.yml
        run: |
          conda env export > environment.yml
      - name: Commit and push changes
        uses: stefanzweifel/git-auto-commit-action@v5
        with: 
          commit_message: "Update requirements.txt from environment.yml"
          file_pattern: "environment.yml"
