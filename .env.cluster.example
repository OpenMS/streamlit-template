# Redis Cluster Configuration Example
# Copy this file to .env and modify for your deployment

# =============================================================================
# Redis Mode Selection
# =============================================================================
# Options: 'standalone' (default), 'cluster', 'sentinel'
REDIS_MODE=cluster

# =============================================================================
# Standalone Mode (default - embedded in container)
# =============================================================================
# For single Redis instance (development/simple deployments)
# REDIS_MODE=standalone
# REDIS_URL=redis://localhost:6379/0
# REDIS_EXTERNAL=false

# =============================================================================
# External Standalone Mode
# =============================================================================
# For connecting to an external single Redis instance
# REDIS_MODE=standalone
# REDIS_URL=redis://your-redis-host:6379/0
# REDIS_EXTERNAL=true

# =============================================================================
# Cluster Mode
# =============================================================================
# For Redis Cluster with multiple shards (horizontal scaling)
# REDIS_MODE=cluster
# REDIS_CLUSTER_NODES=redis-cluster-1:7001,redis-cluster-2:7002,redis-cluster-3:7003

# For local development with docker-compose:
REDIS_CLUSTER_NODES=redis-cluster-1:7001,redis-cluster-2:7002,redis-cluster-3:7003,redis-cluster-4:7004,redis-cluster-5:7005,redis-cluster-6:7006

# For AWS ElastiCache Cluster Mode:
# REDIS_CLUSTER_NODES=your-cluster.xxxxx.clustercfg.region.cache.amazonaws.com:6379

# =============================================================================
# Sentinel Mode
# =============================================================================
# For Redis Sentinel (high availability with automatic failover)
# REDIS_MODE=sentinel
# REDIS_SENTINEL_HOSTS=sentinel1:26379,sentinel2:26379,sentinel3:26379
# REDIS_SENTINEL_MASTER=mymaster

# For local development with docker-compose:
# REDIS_SENTINEL_HOSTS=redis-sentinel-1:26379,redis-sentinel-2:26380,redis-sentinel-3:26381
# REDIS_SENTINEL_MASTER=mymaster

# =============================================================================
# Authentication (optional)
# =============================================================================
# REDIS_PASSWORD=your-redis-password

# =============================================================================
# SSL/TLS (optional, required for some cloud providers)
# =============================================================================
# REDIS_SSL=true

# =============================================================================
# Connection Pool Settings
# =============================================================================
# Maximum connections in the pool (default: 10)
REDIS_POOL_MAX_CONNECTIONS=20

# =============================================================================
# Worker Configuration
# =============================================================================
# Number of RQ workers to start (default: 1)
RQ_WORKER_COUNT=4

# =============================================================================
# GitHub Configuration (for downloading releases)
# =============================================================================
# GITHUB_TOKEN=your-github-token
