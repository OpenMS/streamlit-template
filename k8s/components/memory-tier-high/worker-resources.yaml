apiVersion: apps/v1
kind: Deployment
metadata:
  name: rq-worker
spec:
  template:
    spec:
      containers:
        - name: rq-worker
          resources:
            requests:
              memory: "2Gi"
              cpu: "2"
            limits:
              memory: "180Gi"
              cpu: "20"
